HOOK_API_UPSTREAM=http://127.0.0.1:18480
HOOK_WEB_ORIGIN=http://127.0.0.1:4317
HOOK_TING_UPSTREAM=https://backend.ting.teamofsilicons.com
HOOK_IAM_API_UPSTREAM=https://backend.iam.teamofsilicons.com
HOOK_IAM_AUTHORIZE_ORIGIN=https://auth.iam.teamofsilicons.com
# Optional existing IAM bundle containing exactly Hook and Ting. Otherwise the
# internal console uses IAM batch login. This setting does not create a bundle.
# HOOK_IAM_BUNDLE_ID=tos>your-console-bundle
# Production: persistent private volume, plus a 32-byte base64 encryption key.
# HOOK_SESSION_DIR=/var/lib/silicon-hook-web/sessions
# HOOK_SESSION_KEY=
PORT=4317
